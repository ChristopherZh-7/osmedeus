# Osmedeus DSH Plugin

This package is the compatibility boundary between Osmedeus and DeepSeek
Harness. It does not replace the native DSH Workspace or Session services.

The initial client capability opens a native session named by the
`osmSession` query parameter after DSH has loaded its Session baseline. Future
Osmedeus Assets, Skills, scoped tools, and finding submission capabilities
belong in this package.
