/**
 * Osmedeus bridge, host half.
 *
 * Workspace and Session creation deliberately use DSH's public Host API from
 * Osmedeus. The host apply remains empty so this plugin does not replace or
 * patch the native Workspace service.
 */
export function apply() {}
