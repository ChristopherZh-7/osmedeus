version: '3.8'

# Golish Master-Worker Architecture with Redis
# Usage:
#   docker-compose -f build/docker/docker-compose.yml up -d
#   docker-compose -f build/docker/docker-compose.yml up -d --scale worker=5

services:
  # Redis - Message queue for distributed task processing
  redis:
    image: redis:7-alpine
    container_name: golish-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - golish-network

  # Master Node - Coordinates tasks and exposes API
  master:
    build:
      context: ../..
      dockerfile: build/docker/Dockerfile
    image: golish:latest
    container_name: golish-master
    restart: unless-stopped
    ports:
      - "8002:8002"
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    volumes:
      - golish-data:/root/golish-base
      - workspaces:/root/workspaces-golish
    depends_on:
      redis:
        condition: service_healthy
    command: ["serve", "--master", "-A"]
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8002/health"]
      interval: 30s
      timeout: 10s
      start_period: 10s
      retries: 3
    networks:
      - golish-network

  # Worker Nodes - Execute distributed tasks
  worker:
    build:
      context: ../..
      dockerfile: build/docker/Dockerfile
    image: golish:latest
    restart: unless-stopped
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    volumes:
      - golish-data:/root/golish-base
      - workspaces:/root/workspaces-golish
    depends_on:
      redis:
        condition: service_healthy
      master:
        condition: service_healthy
    command: ["worker", "join", "--redis-url", "redis://redis:6379"]
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: '1'
          memory: 1G
        reservations:
          cpus: '0.5'
          memory: 512M
    networks:
      - golish-network

volumes:
  redis-data:
    driver: local
  golish-data:
    driver: local
  workspaces:
    driver: local

networks:
  golish-network:
    driver: bridge
