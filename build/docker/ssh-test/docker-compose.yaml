services:
  ssh-test:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: golish-ssh-test
    ports:
      - "2222:22"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "pgrep", "sshd"]
      interval: 10s
      timeout: 5s
      retries: 3
