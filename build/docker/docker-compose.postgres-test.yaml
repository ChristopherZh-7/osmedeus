version: '3.8'

# Simple PostgreSQL setup for testing database schema
# Usage:
#   docker-compose -f docker-compose.postgres-test.yaml up -d
#   golish db seed --config build/docker/golish-settings.postgres-test.yaml
#   docker-compose -f docker-compose.postgres-test.yaml down -v

services:
  postgres:
    image: postgres:16-alpine
    container_name: golish-postgres-test
    environment:
      POSTGRES_USER: golish
      POSTGRES_PASSWORD: test_password_123
      POSTGRES_DB: golish
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U golish"]
      interval: 5s
      timeout: 3s
      retries: 5
    volumes:
      - postgres-test-data:/var/lib/postgresql/data

volumes:
  postgres-test-data:
    driver: local
